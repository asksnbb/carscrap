
import type { Metadata } from "next";
import { Geist, Geist_Mono, Pacifico } from "next/font/google";
import "./globals.css";

const pacifico = Pacifico({
  weight: '400',
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-pacifico',
})

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "CashCars - Scrap Car Removal & Cash for Junk Cars | Free Towing",
  description: "Get instant cash for your scrap car! Professional car removal service with free towing, top dollar offers, and eco-friendly recycling. Licensed auto scrapyard serving your area. Call now for a free quote!",
  keywords: "scrap car removal, cash for junk cars, car scrapping near me, sell scrap car, free car towing, auto recycling, junk car removal, instant cash for cars",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning={true}>
      <body
        className={`${geistSans.variable} ${geistMono.variable} ${pacifico.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
